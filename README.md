# 🎬 Beat-Sync Cutter 🎵

> ⚠️ **IMPORTANT NOTICE / DISCLAIMER:**  
> This project is 100% **AI Slop** generated by an LLM during a late-night session.  
> 🚨 **I WILL NOT MAINTAIN THIS REPOSITORY.** No issue tracking, no bug fixes, no feature updates. It works on my machine™ and is shared "as-is".  
>  
> **Why release it then?**  
> Because the output of the LLM actually turned out to be **holy peak / insanely good** for syncing cinematic clips to music beats. Since there are endless overpriced, paid subscription services charging money for basic beat-sync video cutting, I decided to bless the internet with this free, open-source script. Use it, fork it, break it, enjoy it! 🤖✨

---

Automatically cut and sync **cinematic video clips** (Minecraft, gameplay, montages, B-roll) exactly to the **beats, kick drums, snares, and synth drops** of any song!

No manual editing in Premiere Pro, DaVinci Resolve, or CapCut needed. One single command syncs your video to the music. 🚀

---

## 🐣 Step-by-Step Beginner Guide / Anleitung für Einsteiger

Never used Python or the Terminal before? No worries! Follow these steps:

### 1️⃣ Install Prerequisites

#### 🐧 On Linux (Ubuntu/Debian)
Open your terminal (`Ctrl` + `Alt` + `T`) and run:
```bash
sudo apt update && sudo apt install -y python3 python3-pip ffmpeg git
```

#### 🪟 On Windows
1. Download & install [Python](https://www.python.org/downloads/) (Make sure to check **"Add Python to PATH"** during installation!).
2. Download [FFmpeg](https://ffmpeg.org/download.html) and add it to your System Environment Variables (PATH).

---

### 2️⃣ Clone this Repository

Open your Terminal / Command Prompt and run:

```bash
git clone https://github.com/DasFletchi/Beat-Sync-Cutter.git
cd Beat-Sync-Cutter
```

---

### 3️⃣ Install Required Python Packages

Run this command inside the project folder:

```bash
pip install librosa numpy soundfile
```
*(If you see an "externally-managed-environment" error on Linux, create a virtualenv first: `python3 -m venv venv && source venv/bin/activate`)*

---

### 4️⃣ Synchronize your Video! ✨

Copy your **video file** and your **song (MP3/WAV)** into the folder (or use absolute paths).

Then run:

```bash
python beat_sync_cutter.py --video "YourVideo.mp4" --audio "YourSong.mp3" --output "SyncedOutput.mp4"
```

> 💡 **Tip:** If your filenames contain spaces, always wrap them in quotation marks `" "` (as shown above).

---

## ⚙️ Options & Parameters

| Parameter | Description | Default / Example |
|-----------|-------------|-------------------|
| `--video` / `-v` | **(Required)** Path to input video | `--video "gameplay.mp4"` |
| `--audio` / `-a` | **(Required)** Path to input audio track | `--audio "song.mp3"` |
| `--output` / `-o` | Path for final synced video | `--output "synced.mp4"` |
| `--min-cut-dur` | Minimum clip length in seconds (prevents seizure-inducing flickering) | `--min-cut-dur 0.35` |
| `--scale` | Output video resolution via FFmpeg | `--scale 1920:-2` |

---

## 📜 License
MIT License - Do whatever you want with it! 🤖
